
export pure func parseBoolean(s: string): boolean? =>
    if s === 'true' {
        true
    } else if s === 'false' {
        false
    } else {
        nil
    }

export pure js func stringifyBoolean(b: boolean): string => jsGlobal.String(b)